const LoadingDots = () => {
  return (
    <span className={"loadingDots"}>
      <span className="bg-current" />
      <span className="bg-current" />
      <span className="bg-current" />
    </span>
  );
};

export default LoadingDots;
